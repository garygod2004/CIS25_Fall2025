# Week 6 – Strings and Classes

## Overview
This project demonstrates how to use **C++ classes, file input/output, and strings**.  
We create a class called `Item` that stores the **name** and **quantity** of a tool. The program allows saving items to a file (`items.txt`) and loading them back.
